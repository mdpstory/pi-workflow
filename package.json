{
  "name": "pi-workflow",
  "version": "0.1.0",
  "description": "Role-enforced, stage-gated AI workflow extension for pi. Implements ai-workflow-specification.md.",
  "keywords": ["pi-package"],
  "type": "module",
  "scripts": {
    "test": "for t in tests/*.test.mjs; do node --test \"$t\" || exit 1; done && for f in tests/spike-test.mjs tests/concurrency-test.mjs tests/preapproval-test.mjs tests/hooks-approval-test.mjs tests/knowledge-test.mjs tests/e2e-toy.mjs; do node \"$f\" || exit 1; done"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": ["./index.ts"],
    "skills": ["./skills"],
    "prompts": ["./prompts"]
  }
}
